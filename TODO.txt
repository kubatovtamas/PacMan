DJ Isaac bitches
// 1. make the game area 20 x 14
// 2. draw the sketch of the game area
    // remove random ice generator
    // fill with white
    // create walls css + idx
    // make pill css + idx
    // make big pill + idx
    // make pacman start from[9,8]
    // make ghost css + idx
    // make door css + idx
// 3. edge detection
// 4. make tunnels work
// 4. make controls loop

//5.// add sprite for food
    // add sprite for powerup
    // add spirte for ghosts
    // change map array accordingly

//6  Pickup detection:
    //on step: remove class (make it disappear)
//7  count score
//8display score


//1. have lives
//2. display lives
//3. set timer
//4. display timer
//5. top score: local storage
6. win condition: no more food: stop intervals update topscore
7. ghost movement: move, if stuck, random direction, move ...
8. on collision: lives--, everything reset position
9. panic mode: on collision reverse, ghosts killed for 5 sec, score: killing gives points
10. add bg music
+1 cosmetic: mirroring

