DJ Isaac bitches
// 1. make the game area 20 x 14
// 2. draw the sketch of the game area
    // remove random ice generator
    // fill with white
    // create walls css + idx
    // make pill css + idx
    // make big pill + idx
    // make pacman start from[9,8]
    // make ghost css + idx
    // make door css + idx
// 3. edge detection
// 4. make tunnels work
// 4. make controls loop

//5.// add sprite for food
    // add sprite for powerup
    // add spirte for ghosts
    // change map array accordingly

//6  Pickup detection:
    //on step: remove class (make it disappear)
//7  count score
//8display score


//1. have lives
//2. display lives
//3. set timer
//4. display timer
//5. top score: local storage
//1.  display main menu
//    display controls
//2.  have a play button (start audio) -> P







1.  win condition:
        //test: win cond: 10 score
            //replace pac and ghost with peepoclap
            //stop intervals
        production: 131 score
2.  ghost movement: move, if stuck, random direction, move ...
3.  on collision:
        //lives--,
        //pacman reset
        ghosts reset position
4.  panic mode: on collision reverse, ghosts killed for 5 sec, score: killing gives points
5.  actually have audio: background music

+1 cosmetic: mirroring


