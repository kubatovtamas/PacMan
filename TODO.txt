// 1. make the game area 20 x 14
// 2. draw the sketch of the game area
    // remove random ice generator
    // fill with white
    // create walls css + idx
    // make pill css + idx
    // make big pill + idx
    // make pacman start from[9,8]
    // make ghost css + idx
    // make door css + idx
3. make controls loop
4. edge detection
